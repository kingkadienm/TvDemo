<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TvDemo"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DemoRecyclerViewActivity"/>
        <activity android:name=".DemoGridViewActivity"/>
        <activity android:name=".DemoListViewActivity"/>
        <activity android:name=".DemoTabActivity"/>
        <activity android:name=".DemoFragmentActivity"/>
        <activity android:name=".DemoTwoRecyclerViewActivity"/>
        <activity android:name=".DemoHomeActivity"/>
        <activity android:name=".DemoTopBorderActivity"/>
        <activity android:name=".DemoMenuActivity"/>

    </application>

</manifest>